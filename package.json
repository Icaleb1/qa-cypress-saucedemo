{
  "name": "qa-cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run cy:run:chrome",

    "cy:clean:reports": "rm -rf cypress/reports/*",
    "cypress:open:dev:chrome": "npx cypress open --config-file config-dev.js --browser chrome",
    "cy:run:dev:chrome": "npx cypress run --config-file config-dev.js --browser chrome"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "cypress": "^15.17.0",
    "cypress-mochawesome-reporter": "^4.0.2",
    "cypress-multi-reporters": "^2.0.5",
    "dotenv": "^17.4.2",
    "mocha-junit-reporter": "^2.2.1"
  }
}
